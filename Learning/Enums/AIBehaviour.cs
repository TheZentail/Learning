using System;

namespace Learning
{
    // Just a data storing enumeration because "NONE", "PASSIVE', "AGGRESSIVE" are easier to
    // reference and read than using something like 0, 1, 2.
	public enum AIBehaviour
	{
        NONE,
        PASSIVE,
        AGGRESSIVE
    }
}

